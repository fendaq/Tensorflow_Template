
{
  "max_epochs": 2,
  "batch_size": 16,
  "optimizer": "SGD",
  "learning_rate": 0.1,
  "num_units": 512,
  "keep_checkpoint_max": 5,
  "output_dir": "./outputs/",
  "checkpoint_name": "example_model"
}
